
subprojects {
  task codeformatCheck(type:Exec) {
    commandLine "${project.projectDir.toString}/scalafmt", "--test", "."
  }

  task codeformatFix(type:Exec) {
    commandLine "${project.projectDir.toString}/scalafmt", "."
  }
}
