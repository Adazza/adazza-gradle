
subprojects {
  task codeformatCheck(type:Exec) {
    commandLine "${project.projectDir.getAbsolutePath()}/scalafmt", "--test", "."
  }

  task codeformatFix(type:Exec) {
    commandLine "${project.projectDir.getAbsolutePath()}/scalafmt", "."
  }
}
